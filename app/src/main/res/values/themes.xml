<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- AppCompat parent enables per-app locale switching (Dari/Pashto/English)
         via AppCompatDelegate; Compose owns all visual theming. -->
    <style name="Theme.WorkTrack" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
